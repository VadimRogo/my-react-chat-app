# Project Blueprint

## Overview

This document outlines the plan for creating a Mini App-optimized login and registration page.

## Current Goal: Implement User Authentication

### Plan

1.  **DONE** ~~Create a `Profile.jsx` component.~~
2.  **DONE** ~~Update `App.jsx` to include a route for the profile page.~~
3.  **DONE** ~~Add state to `App.jsx` to manage authentication status.~~
4.  **DONE** ~~Modify `Login.jsx` to handle authentication logic.~~

### New Goal: Add a second registration page

1.  **DONE** ~~Create a `SignUpDetails.jsx` component for additional user information.~~
2.  **DONE** ~~Add a new route for the `SignUpDetails` page in `App.jsx`.~~
3.  **DONE** ~~Modify `SignUp.jsx` to navigate to the `SignUpDetails` page after the initial registration.~~

### New Goal: Display user data on the profile page

1.  **DONE** ~~Update `App.jsx` to hold user details in state.~~
2.  **DONE** ~~Update `SignUpDetails.jsx` to pass user details to `App.jsx`.~~
3.  **DONE** ~~Update `Profile.jsx` to display the user's first name, last name, and age.~~

### New Goal: Request date of birth instead of age

1.  **DONE** ~~Update `SignUpDetails.jsx` to request the user's date of birth.~~
2.  **DONE** ~~Update `Profile.jsx` to display the user's date of birth and calculated age.~~
